<div class="text">
    {{message.text}}
</div>

<div class="time">

</div>

{{#if imageUrls}}
    <div class="images">
        {{#each url in imageUrls}}
            {{message-image url=url}}
        {{/each}}
    </div>
{{/if}}
