<div class="rooms">
    {{irc-room room=connection.serverRoom target=controller}}

    {{#each room in rooms}}
        {{irc-room room=room target=controller}}
    {{/each}}
</div>

<div class="messages">
    <div class="message-container">
        {{#each group in messageGroups}}
            {{irc-message-group group=group}}
        {{/each}}
    </div>

    <div class="text-box">
        {{textarea value=currentMessage classNames="form-control"}}
    </div>
</div>

<div class="roster">
    {{#each nickname in roomNicknames}}
        <div class="nickname">
            {{nickname}}
        </div>
    {{/each}}

    <div class="spare"></div>
</div>
